import Guider from "./Guider";

import {
  defaultOption,
  getDefaultOption,
  GuiderOption,
  IGuider,
  Status,
  Step,
} from "./types";

import { animationCss, createStyles } from "./styles";

export default Guider;
export { defaultOption, getDefaultOption, createStyles, animationCss };
export type { GuiderOption, IGuider, Status, Step };
